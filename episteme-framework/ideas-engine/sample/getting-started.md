# Getting started

Sample topic file for the Ideas System. Copy this to `~/ideas/getting-started.md` (or use as reference) to see the format. Real idea files live in `~/ideas/` (or IDEAS_ROOT).

---

## Entry 1

**Verbatim:** "Use Episteme as a base fork: add AGENTS.md at repo root, point to meta-agent, then pull in only the skills and docs you need."
**Agent:** Episteme is designed to be forked and personalized. The bootstrap (AGENTS.md) stays small; the meta-agent and skills live in the framework. Copy or link the bootstrap into a new project and point it at this repo (or your fork) so the agent knows where to find the library.

id: 1 · created_at: 2026-03-03T12:00:00Z

---

## Entry 2

**Verbatim:** "Ideas are one topic per file, append-only. Verbatim is immutable."
**Agent:** Each topic file in ~/ideas/ holds multiple entries. Every entry has Verbatim (user text, never edited) and an agent interpretation. Append-only keeps a clear history; for merge/split, create new files and optionally archive old ones.

id: 2 · created_at: 2026-03-03T12:05:00Z
